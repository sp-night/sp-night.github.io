## What

<!-- One paragraph: what changes and why. -->

## Checklist

- [ ] `npm run check && npm test` pass locally — the contrast policy is part of the tests
- [ ] No hex literal outside `src/data/` (the no-raw-hex test enforces this)
- [ ] If the palette changed: `npm run assets` was run and the regenerated images are committed
